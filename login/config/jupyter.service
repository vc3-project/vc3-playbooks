[Unit]
Description=Jupyter Notebook

[Service]
Type=simple
PIDFile=/run/jupyter.pid
ExecStart=/usr/bin/jupyter-notebook --config="/etc/.jupyter/jupyter_notebook_config.py"
User=jerryzhou
WorkingDirectory=/home/
Restart=always
RestartSec=10
#KillMode=mixed

[Install]
WantedBy=multi-user.target